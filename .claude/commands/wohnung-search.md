---
description: Search Vienna rental portals for new apartments matching the configured profile and produce a ranked report
---

Invoke the `wohnung-search` skill: run a fresh apartment search across the configured
portals, evaluate the new listings (including their photos), and write a ranked report.

Optional argument: a number of pages to sweep per source (default 3). Example: `/wohnung-search 5`.

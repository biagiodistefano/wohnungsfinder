---
description: First-run interview — create your personal search profile (language, budget, districts, must-haves) and install dependencies
---

Invoke the `setup` skill: run the guided interview, write `criteria.toml`
(personal, gitignored), install dependencies with uv, and optionally set up
scheduled searches.

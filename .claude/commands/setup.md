---
description: First-run interview — create your personal search profile (rent or buy, language, budget, districts, must-haves, wish list, email delivery) and install dependencies
---

Invoke the `setup` skill: run the guided interview, write `criteria.toml`
(personal, gitignored), optionally configure email delivery (`.env`), install
dependencies with uv, and optionally set up scheduled searches.
